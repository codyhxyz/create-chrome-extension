# Chrome Web Store Submission

## Prerequisites

- **Chrome Web Store developer account** -- [$5 one-time registration fee](https://chrome.google.com/webstore/devconsole/register)
- **Privacy policy URL** -- must be publicly accessible before submission (see [templates/privacy-policy.md](./templates/privacy-policy.md))
- **Extension icons** -- 128x128 minimum (auto-generated by `@wxt-dev/auto-icons` from source)
- **Screenshots** -- at least one at 1280x800 or 640x400

## Pre-flight

Run the ship-mode validator before every submission:

```bash
npm run check:cws:ship
```

This is structural checks (which CI also runs via `npm run check:cws`) plus submission-readiness checks (real name, real description — no factory placeholders). Zero errors is required before you run `npm run zip`.

See [09-cws-best-practices.md](./09-cws-best-practices.md) for the full rule list, the design philosophy behind the two tiers, and the "why" behind each rule — including things the Chrome Web Store enforces but never writes down in one place (like runtime host-permission requests for faster review).

## Manual Submission

### 1. Build the zip

```bash
npm run zip
```

Output: `.output/<name>-<version>-chrome.zip`

### 2. Upload to CWS

1. Go to the [Chrome Web Store Developer Dashboard](https://chrome.google.com/webstore/devconsole)
2. Click "New Item" (or update an existing one)
3. Upload the zip file

### 3. Fill listing fields

Use the templates in `docs/templates/` to prepare:
- [store-listing.md](./templates/store-listing.md) -- name, description, category
- [privacy-policy.md](./templates/privacy-policy.md) -- required privacy policy
- [cws-submission-checklist.md](./templates/cws-submission-checklist.md) -- full pre-flight checklist

### 4. Upload screenshots

- 1280x800 or 640x400 (1-5 required)
- See [05-launch-materials.md](./05-launch-materials.md) for asset specs

### 5. Submit for review

Review typically takes 1-3 business days. You'll get an email when approved or rejected.

## Automated publishing: `npm run ship`

One command runs the full flow: validate → compare versions → build+zip → upload → publish → poll until terminal state.

```bash
npm run ship
```

Which expands to:

```
npm run check:cws:ship   # 16 rules: structural + listing/welcome readiness + listing drift
tsx scripts/version-sync.ts  # local package.json version > live CWS version
wxt zip                  # build and package for CWS
tsx scripts/publish-cws.ts   # upload, publish, poll status
```

The first failing gate halts the pipeline. You never produce a zip that isn't ship-ready, and you never push a version that doesn't bump the live one.

### Secrets setup (opt-in)

The publish step (and the `listing-drift` validator rule, and `version-sync`) need the same 4 OAuth secrets as the keepalive workflow:

- `CWS_EXTENSION_ID`
- `CWS_CLIENT_ID`
- `CWS_CLIENT_SECRET`
- `CWS_REFRESH_TOKEN`

See [08-keepalive-publish.md](./08-keepalive-publish.md) for the one-time OAuth setup — same credentials, same guide.

**Without these secrets set, every CWS API step no-ops cleanly.** `version-sync` prints "skipped" and exits 0. `publish-cws` prints a "configure CWS_* secrets to enable" message and exits 0. `listing-drift` returns nothing. Forks of the factory never see a red CI badge because they haven't pasted OAuth tokens.

### Setting secrets locally

For local runs, export them in your shell, use direnv, or put them in a git-ignored `.env` file consumed by your shell:

```bash
export CWS_EXTENSION_ID="abcdefghijklmnopqrstuvwxyz123456"
export CWS_CLIENT_ID="xxx.apps.googleusercontent.com"
export CWS_CLIENT_SECRET="xxx"
export CWS_REFRESH_TOKEN="xxx"
npm run ship
```

### What `publish-cws` reports

The script emits state transitions on stdout (`uploading`, `uploaded`, `publishing`, `in-review`, `live`, `rejected`, `failed`, `timeout`) with timestamps. Use `--json` for a single envelope that skills or CI jobs can parse.

Review typically completes in 1–3 business days. If polling times out (~15 minutes default), check the CWS dashboard for final status.

### Alternate: `wxt submit`

WXT also has a built-in submit command with its own env var names (`CHROME_EXTENSION_ID`, `CHROME_CLIENT_ID`, etc.). Use `npm run ship` for the gated pipeline; `wxt submit` is the bare submission if you want to skip the gates.

## Common Rejection Reasons

| Reason | Fix |
|---|---|
| Missing privacy policy | Add a publicly accessible privacy policy URL |
| Unjustified permissions | Remove permissions you don't use, or explain each one in the privacy practices section |
| Vague single purpose | Write a clear, specific one-sentence purpose description |
| Remote code execution | Remove any `fetch()` + `eval()` patterns, no loading remote scripts |
| Misleading description | Make the listing accurately reflect what the extension does |
| Broken functionality | Test the production build (`npm run build` + load unpacked) before submitting |

**You get one appeal per violation.** Ship clean the first time.

## Updating an Existing Extension

1. Bump version in `package.json`
2. `npm run zip`
3. Go to CWS dashboard > your extension > "Package" tab
4. Upload the new zip
5. Submit for review

See [04-staleness-prevention.md](./04-staleness-prevention.md) for keeping your extension active.
